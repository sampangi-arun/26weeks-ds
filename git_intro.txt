1) create a repo in github
2) to create a local repo use below commands
	git init
3) in git bash terminal fire below command to link  the local
   and remote repo
	git remote add origin <<repo link>>.
4) make a change to the local repo
        git add <<file name>>
        git status
        git commit -m "message"
